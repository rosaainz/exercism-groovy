class TwoFer {

    static String twoFer(String name){
      name == null || name.isEmpty() ? 'One for you, one for me.' : "One for $name, one for me."
//      if(name == null || name.isEmpty
//        return 'One for you, one for me.'
//      }else{
//         return "One for $name, one for me."
//      }
    }
}
